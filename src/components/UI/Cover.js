import React from 'react';
import Button from 'react-bootstrap/Button'
export const Cover=props=>{
    return (
        <div className="cover" id="home">
            <h3>Create or take Quiz on a fly!</h3>
            <Button variant="outline-custom">Get Started</Button>
        </div>
    );
}