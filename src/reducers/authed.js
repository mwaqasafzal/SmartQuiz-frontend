
const initialState={
    username:"mwaqas",
    fullName:"Muhammad Waqas"
}
export default function(state=initialState,action){
    switch(action.type){
        default:
            return state;
    }
}